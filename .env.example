# Token exigido para ler os leads em GET /api/leads (header: x-api-key)
LS_ADMIN_TOKEN=troque-este-token

# Para onde os leads devem ser encaminhados no futuro (opcional por enquanto)
LS_LEADS_WEBHOOK_URL=
